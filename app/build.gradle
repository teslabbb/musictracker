apply plugin: 'com.android.application'

android {
    namespace 'com.example.ytmusictracker'
    compileSdk 34

    defaultConfig {
        applicationId "com.example.ytmusictracker"
        minSdkVersion 21
        targetSdkVersion 34
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }
}
